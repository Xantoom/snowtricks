<nav class="navbar navbar-expand-md navbar-light bg-light shadow-sm d-none d-md-flex">
    <div class="container">
        <!-- Logo -->
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logo.png') }}" alt="Logo" height="40">
        </a>

        <!-- Navigation Links -->
        <div class="d-flex align-items-center">
            <a href="{{ path('app_home') }}" class="nav-link px-3 fw-medium">
                <i class="fas fa-home me-1"></i>Home
            </a>
            <div class="vr mx-2"></div>
            <a href="{{ path('app_home') }}" class="nav-link px-3 fw-medium">
                <i class="fas fa-snowboarding me-1"></i>Tricks
            </a>

            {% if app.user %}
                <div class="vr mx-2"></div>
                <a href="{{ path('app_logout') }}" class="nav-link px-3 fw-medium">
                    <i class="fas fa-sign-out-alt me-1"></i>Logout
                </a>
                <span class="ms-3 badge bg-primary rounded-pill">
                    <i class="fas fa-user me-1"></i>{{ app.user.email }}
                </span>
            {% else %}
                <div class="vr mx-2"></div>
                <a href="{{ path('app_register') }}" class="nav-link px-3 fw-medium">
                    <i class="fas fa-user-plus me-1"></i>Sign up
                </a>
                <div class="vr mx-2"></div>
                <a href="{{ path('app_login') }}" class="nav-link px-3 fw-medium">
                    <i class="fas fa-sign-in-alt me-1"></i>Sign in
                </a>
            {% endif %}
        </div>
    </div>
</nav>
